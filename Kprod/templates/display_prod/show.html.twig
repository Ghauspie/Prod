{% extends 'base.html.twig' %}

{% block title %}DisplayProd{% endblock %}

{% block body %}
    <h1>DisplayProd</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ display_prod.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ display_prod.name }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ display_prod.url }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ display_prod.createdAt ? display_prod.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ display_prod.updatedAt ? display_prod.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

     <button class="btn btn-primary me-md-2"><a href="{{ path('display_prod_index') }}">back to list</a></button>

    <button class="btn btn-primary me-md-2"> <a href="{{ path('display_prod_edit', {'id': display_prod.id}) }}">edit</a></button>

    {{ include('display_prod/_delete_form.html.twig') }}
{% endblock %}
