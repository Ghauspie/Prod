<nav class="nav">

{% if app.user %}
   <button type="button" class="btn button  size-button"><a class="nav-link size-nav" href="{{ path('app_logout') }}">logout</a></button>
    {% if is_granted('ROLE_ADMIN') %}
    <button type="button" class="btn button size-button"><a class="nav-link active size-nav" href="{{ path('main') }}">Acceuil</a></button>
    <button type="button" class="btn button size-button"><a class="nav-link size-nav" href="{{ path('user_index') }}">User</a></button>
    <button type="button" class="btn button size-button"><a class="nav-link size-nav" href="{{ path('display_prod_index') }}">Display Prod</a></button>
    {% endif %}
{% else %}
 <button type="button" class="btn button size-button"><a class="nav-link size-nav" tabindex="+1" href="{{ path('app_login') }}">Login</a></button>
{% endif %}

</nav>